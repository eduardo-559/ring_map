<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RingMap" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!--primary colors -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">#CFF5FF</item>
        <item name="colorOnPrimary">@color/azul_claro</item>

        <!--secondary colors-->

        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">#031DAA</item>
        <item name="colorOnSecondary">@color/azul_escuro</item>
    </style>

    <style name="Theme.RingMap" parent="Base.Theme.RingMap" />

    <style name="Theme.RingMap.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.RingMap.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RingMap.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
  <!-- Base application theme. -->
    <style name="Base.Theme.RingMap.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.RingMap.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.RingMap.NoActionBar.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.RingMap.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>